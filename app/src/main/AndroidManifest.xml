<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.ichwan.bayurid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mitra.MitraDetail"></activity>
        <activity android:name=".buyer.MainClientDetail" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".home.ArtikelDetail" />
        <activity android:name=".home.HomeProductDetail" />
        <activity android:name=".buyer.MainClientActivity" />
        <activity android:name=".mitra.AddMitraActivity" />
        <activity android:name=".produk.AddProdukActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>